install numpy
install matplotlib
install scipy
install fastcluster
install sklearn
install gensim
install nltk
download nltk data 
	import nltk
	nltk.download('stopwords')


if df_apply error comes

EDIT: This can be fixed by replacing import df_apply with import dynamicTreeCut.df_apply. Also replace from R_func import * with from dynamicTreeCut.R_func import *.
pip install pandas
pip install gensim
pip install pyemd

download glove, fasttext, and word2vec models in data folder


Set all the variables in the configuration file

run cluster.py

generated clusters will be saved in the new folder in "results_dynamic" generated in test data folder

Evaluation results will be generated based on the attributes in the configuration file and are saved in /data/silhoute_evaluation.csv.

